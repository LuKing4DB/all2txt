<开头样本>
{{sample_content}}
</开头样本>

## 任务
分析上述<开头样本>，识别文档中的**第一层级标题**（最大层级标题）的格式特征，然后从下面提供的正则表达式选项中选择**最合适的一个**来匹配这些第一层级标题。

## 分析步骤
1. **分析样本中的层级结构**
   - 识别哪些行是标题，哪些是子层级
   - 找出第一层级标题的共同格式特征

2. **识别第一层级标题的格式**
   - 第一层级标题是文档中层级最高的标题（没有父层级）
   - 子层级标题通常包含更多层级标记（如更多点号、更多数字等）
   - **行权重规则：越靠前的行权重越高。第一行权重最高，第二行次之，第三行再次之。优先选择能匹配更靠前的行的选项**

3. **从选项中选择最匹配的正则表达式**
   - **首先检查前面的行：确保选中的选项能匹配样本的前几行（特别是第一行）**
   - 测试每个选项，看哪个能正确匹配样本中的第一层级标题
   - 确保选中的表达式不匹配子层级标题
   - **如果无法匹配任何选项，返回-1，不要强行选择**

## 正则表达式选项
{{regex_options}}

## 选择标准
- **必须匹配前面的行**：选中的表达式必须能匹配样本的前几行（特别是第一行）
- **行权重规则**：如果第一行匹配某个选项，该选项权重最高；如果第二行匹配，权重次之；如果第三行匹配，权重再次之。优先选择能匹配更靠前的行的选项
- **必须匹配第一层级标题**：选中的表达式必须能匹配样本中所有第一层级标题
- **必须排除子层级**：选中的表达式不能匹配任何子层级标题
- **不要强行选择**：如果无法匹配任何选项，返回-1，不要强行选择一个不匹配的选项

## 输出要求
**只返回选项编号（从1开始的整数），或者-1（表示都不匹配）**

**重要提示：**
- 如果无法匹配任何选项，返回`-1`，不要强行选择
- 只返回选项编号，不要包含任何其他内容
- 不要包含代码块标记、解释、说明、注释
- 不要包含"选项"、"编号"等文字前缀
- 不要返回多个选项

**输出格式示例：**
- 正确格式：`5`、`3`、`-1`
- 错误格式：`选项5`、`5:85`、`5 85`

